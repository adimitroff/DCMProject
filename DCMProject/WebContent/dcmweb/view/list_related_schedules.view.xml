<core:View xmlns:core="sap.ui.core" xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m" controllerName="net.cb.dcm.frontend.controller.list_related_schedules"
	xmlns:html="http://www.w3.org/1999/xhtml">
	<Page title="Playlist Schedules" showNavButton="true"
    	navButtonTap="onNavigateBack">
		<headerContent>
			<Button icon="sap-icon://add" text="Add Relation" press="onAdd" />
		</headerContent>
		<Table id="idMediaContentsTable" inset="false" items="{/Playlists(2L)/PlaylistScheduleDetails}"
			itemPress="rowSelected" mode="Delete" delete="onDelete">
			<headerToolbar>
				<Toolbar>
					<Label text="Related Tags"></Label>
				</Toolbar>
			</headerToolbar>
			<columns>
				<Column>
					<Text text="Id" />
				</Column>
				<Column minScreenWidth="Tablet" demandPopin="true">
					<Text text="Type" />
				</Column>
				<Column minScreenWidth="Tablet" demandPopin="true" hAlign="Right">
					<Text text="Start Time" />
				</Column>
				<Column minScreenWidth="Tablet" demandPopin="true" hAlign="Right">
					<Text text="End Time" />
				</Column>
				<Column minScreenWidth="Tablet" demandPopin="true" hAlign="Right">
					<Text text="Date" />
				</Column>
				<Column minScreenWidth="Tablet" demandPopin="true" hAlign="Right">
					<Text text="Month" />
				</Column>
				<Column minScreenWidth="Tablet" demandPopin="true" hAlign="Right">
					<Text text="Day of month" />
				</Column>
				<Column minScreenWidth="Tablet" demandPopin="true" hAlign="Right">
					<Text text="Day of week" />
				</Column>
			</columns>
			<items>
				<ColumnListItem>
					<cells>
						<ObjectIdentifier title="{Id}" />
						<Text text="{Type}" />
						<Text id="StartTime" text="{path: 'StartTime', formatter: '.formatTime'}" />
						<Text id="EndTime" text="{path: 'EndTime', formatter: '.formatTime'}" />
						<Text text="{Date}" />
						<Text text="{Month}" />
						<Text text="{DayOfMoth}" />
						<Text text="{DayOfWeek}" />
					</cells>
				</ColumnListItem>
			</items>
		</Table>
	</Page>
</core:View>